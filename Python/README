run inside ipython with:

run CombinationFilter.py

Or run directly from the terminal with:

ipython -i --matplotlib tk CombinationFilter.py

If this doesn't work, comment out:

plt.ion()

This will turn of interactive mode so you won't be able to use plt.ginput() to extract data from the plot.



